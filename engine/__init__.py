"""A reusable, config-driven interior design engine.

Room geometry, measured drawings, render prompts, render backends and the report are all
derived from one project config, so nothing about a room is ever written down twice.
"""
__version__ = "1.0.0"
